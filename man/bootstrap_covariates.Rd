% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootstrap_covariates}
\alias{bootstrap_covariates}
\title{Bootstrap Standard Errors for Covariate Model Parameters}
\usage{
bootstrap_covariates(
  model_state,
  X,
  Y,
  n_reps = 100,
  random_state = 123,
  ref_class = 1
)
}
\arguments{
\item{model_state}{A fitted \code{mixture_model} object with a covariate
structural model (fitted with \code{structural = "covariate"}).}

\item{X}{Numeric matrix. The original measurement data used to fit
\code{model_state}.}

\item{Y}{Numeric matrix. The original covariate data used to fit
\code{model_state}.}

\item{n_reps}{Positive integer. Number of bootstrap replications.
Default is \code{100}.}

\item{random_state}{Integer seed for reproducibility. Default is \code{123}.}

\item{ref_class}{Integer. Reference class for centering bootstrap betas.
Should match the \code{ref_class} used in subsequent calls to
\code{\link{confint.mixture_model}} and \code{\link{wald_omnibus_test}}.
Default is \code{1}.}
}
\value{
A named list with four elements:
\itemize{
\item \code{standard_errors} K x D numeric matrix of bootstrap standard errors
(one row per class, one column per predictor).
\item \code{p_values} K x D numeric matrix of two-sided p-values.
\item \code{boot_betas} 3-D array of shape \verb{(n_reps, K, D)} containing aligned,
reference-centered bootstrap beta estimates. Required by
\code{\link[=wald_omnibus_test]{wald_omnibus_test()}}.
\item \code{orig_betas} K x D matrix of the original model's beta coefficients,
centered on \code{ref_class}.
}
}
\description{
Estimates standard errors and p-values for the covariate structural model
parameters using nonparametric bootstrap resampling. In each replicate,
a new model is fitted on a bootstrap sample and class labels are aligned
to those of the original model via globally optimal assignment (solving the
linear sum assignment problem by enumeration for K <= 8), correcting for
label switching. Results can be passed directly to
\code{\link{confint.mixture_model}} and \code{\link{wald_omnibus_test}}.
}
\examples{
\dontrun{
set.seed(1)
X <- matrix(rbinom(500, 1, 0.5), nrow = 100)
Z <- matrix(rnorm(100), nrow = 100)
colnames(Z) <- "age"
fit <- fit_mixture(X, Y = Z, n_components = 2, measurement = "binary",
                   structural = "covariate",
                   n_steps = 3, correction = "ML", n_init = 5)
boot <- bootstrap_covariates(fit, X, Z, n_reps = 200)
boot$p_values
confint(fit, boot_results = boot)
}

}
