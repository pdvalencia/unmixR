% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blrt.R
\name{calc_blrt}
\alias{calc_blrt}
\title{Bootstrap Likelihood Ratio Test (BLRT) for Class Enumeration}
\usage{
calc_blrt(
  X,
  k_small,
  k_large,
  measurement = "binary",
  n_reps = 100,
  n_init_base = 20,
  n_init_boot = 10,
  ...
)
}
\arguments{
\item{X}{A numeric matrix of indicator variables (the measurement data).
Rows are observations; columns are items.}

\item{k_small}{Positive integer. The number of classes in the null model.}

\item{k_large}{Positive integer. The number of classes in the alternative
model. Must be strictly greater than \code{k_small}.}

\item{measurement}{Character string specifying the measurement model type.
See \code{\link{fit_mixture}} for accepted values. Default is
\code{"binary"}.}

\item{n_reps}{Positive integer. Number of bootstrap replications. More
replications give a more precise p-value but take longer. Default is
\code{100}.}

\item{n_init_base}{Positive integer. Number of random restarts when fitting
the observed-data models. Default is \code{20}.}

\item{n_init_boot}{Positive integer. Number of random restarts for each
bootstrap replicate pair of models. Default is \code{10}.}

\item{...}{Additional arguments passed to \code{\link{fit_mixture}}.}
}
\value{
A named list with three elements:
\itemize{
\item \code{p_value} Numeric. The bootstrap p-value, computed as
\verb{(#\{null_dist >= obs_diff\} + 1) / (n_reps + 1)}.
\item \code{obs_diff} Numeric. The observed likelihood ratio statistic:
\code{2 * (LL_large - LL_small)}.
\item \code{null_dist} Numeric vector of length \code{n_reps} containing the bootstrap
null distribution of the test statistic.
}
}
\description{
Tests whether a K-class model fits significantly better than a (K-1)-class
model by using a parametric bootstrap to approximate the null distribution
of the likelihood ratio statistic. This avoids the known violation of
standard chi-squared regularity conditions in mixture models, where the
null hypothesis places a parameter on the boundary of the parameter space.

The function fits both the null (\code{k_small}-class) and alternative
(\code{k_large}-class) models on the observed data, computes the observed
likelihood ratio statistic, then generates \code{n_reps} synthetic datasets
under the null model to build the reference distribution.
}
\examples{
\dontrun{
set.seed(1)
X <- matrix(rbinom(500, 1, 0.5), nrow = 100)
result <- calc_blrt(X, k_small = 2, k_large = 3,
                    measurement = "binary", n_reps = 50)
result$p_value
hist(result$null_dist, main = "BLRT Null Distribution")
abline(v = result$obs_diff, col = "red")
}

}
